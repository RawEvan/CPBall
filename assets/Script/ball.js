cc.Class({
    extends: cc.Component,

    properties: {
        lost: false,
    },

    update (dt) {
        // if(this.node.parent.convertToNodeSpaceAR(this.node.position)[0] < 0) {
            // this.node.lost = true;
            // console.log('lost');
        // }
    },
});